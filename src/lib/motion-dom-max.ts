/* `layoutId` / drag kabi ogʻir motion imkoniyatlari (domMax) — alohida
   modul, `import()` bilan talabga koʻra yuklanadi. Qarang: SegmentedToggle. */
export { domMax as default } from "motion/react";
